{
  "name": "venturebridge",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:generate": "turbo db:generate"
  },
  "packageManager": "npm@10.9.2",
  "devDependencies": {
    "turbo": "^2.2.3"
  }
}
